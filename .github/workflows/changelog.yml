name: Check News entry
on:
  pull_request:
    types: [opened, reopened, synchronize, labeled, unlabeled]

jobs:
  Check-Changelog:
    name: Check that the News.md was updated
    runs-on: ubuntu-latest
    if: ${{ !contains(github.event.pull_request.labels.*.name, 'no changelog necessary') }}
    steps:
      - uses: tarides/changelog-check-action@v4
        with:
          changelog: NEWS.md